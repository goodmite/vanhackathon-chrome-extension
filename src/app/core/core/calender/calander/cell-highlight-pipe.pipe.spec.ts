import { CellHighlightPipePipe } from './cell-highlight-pipe.pipe';

describe('CellHighlightPipePipe', () => {
  it('create an instance', () => {
    const pipe = new CellHighlightPipePipe();
    expect(pipe).toBeTruthy();
  });
});
