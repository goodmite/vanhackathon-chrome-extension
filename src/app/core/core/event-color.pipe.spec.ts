import { EventColorPipe } from './event-color.pipe';

describe('EventColorPipe', () => {
  it('create an instance', () => {
    const pipe = new EventColorPipe();
    expect(pipe).toBeTruthy();
  });
});
