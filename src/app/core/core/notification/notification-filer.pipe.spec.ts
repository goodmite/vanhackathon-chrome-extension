import { NotificationFilerPipe } from './notification-filer.pipe';

describe('NotificationFilerPipe', () => {
  it('create an instance', () => {
    const pipe = new NotificationFilerPipe();
    expect(pipe).toBeTruthy();
  });
});
