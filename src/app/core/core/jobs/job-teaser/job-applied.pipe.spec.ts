import { JobAppliedPipe } from './job-applied.pipe';

describe('JobAppliedPipe', () => {
  it('create an instance', () => {
    const pipe = new JobAppliedPipe();
    expect(pipe).toBeTruthy();
  });
});
